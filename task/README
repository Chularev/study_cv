‘matching’ and ‘non-matching’ phrase I linked with unit tests. Because unit tests created before programm they can contains words and phrases that have never used in expressions. For example, ‘non-matching’ phrase list about numbers contain word "Сто" but this word never used in expressions etc.
